<article class="card">
    <div class="card-body">
        <p>{{item.origin}}</p>
        <p>{{item.shortURL}}</p>
        <div class="btn btn-danger">Eliminar</div>
        <div class="btn btn-warning">Editar</div>
        <div class="btn btn-info">Copiar</div>
    </div>
</article>